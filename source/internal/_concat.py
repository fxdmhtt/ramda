# -*- coding: utf-8 -*-

def _concat(set1, set2):
    return list(set1) + list(set2)

    # ignore:

    # set1 = set1 or []
    # set2 = set2 or []
    # len1 = len(set1)
    # len2 = len(set2)
    # result = []

    # idx = 0
    # while idx < len1:
    #     result.append(set1[idx])
    #     idx += 1
    # idx = 0
    # while idx < len2:
    #     result.append(set2[idx])
    #     idx += 1
    # return result
